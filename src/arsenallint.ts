import { createHash, generateKeyPairSync, sign, verify } from "node:crypto";
import { chmodSync, existsSync, openSync, readFileSync, writeFileSync } from "node:fs";
import { execFileSync } from "node:child_process";
import { basename, dirname, resolve } from "node:path";
import ts from "typescript";

export interface Policy { name:string; version:string; roles:{id:string;tools?:string[];irreversible_tools?:string[]}[]; runtime:Record<string,unknown>; guardrails:Record<string,unknown>; claims:string[]; catalog_commit?:string }
export interface Tip { id:string; title?:string; impact:string; verification_status:string; enrichment_status:string }
export interface Catalog { commit?:string; policyPin?:string; contentDigest:string; provenance:{source_type:"git"|"fixture";resolved_path:string;git_commit?:string;content_sha256:string;generated_at?:string;tip_count:number}; tips:Map<string,Tip> }
export interface Finding { rule:string;arsenal_tip_id:string;severity:string;weight:number;evidence:string;verification_status:string }
export interface Verdict { policy:string;status:"FAIL"|"WARN"|"PASS";findings:Finding[];unsubstantiated_claims:string[];cited_tips:string[];seal:string;remediation_plan:unknown;trust_badges:{arsenal_tip_id:string;impact:string;verification_status:string;enrichment_status:string}[] }
export interface Evidence { file:string;line:number;kind:"declared"|"inferred"|"observed"|"unknown";detail:string }
export interface Capabilities { roles:{id:string;tools:string[];irreversible_tools:string[]}[];runtime:Record<string,unknown>;guardrails:Record<string,unknown>;network_tools:string[];code_exec_tools:string[];approval_wrappers:string[];evidence:Evidence[];unsupported:string[] }
export interface ConsistencyFinding { code:string;evidence:string;policy_value?:unknown;source_value?:unknown }
export interface ReceiptCatalogProvenance { source_type:"git"|"fixture";catalog_commit?:string;content_sha256:string;generated_at?:string;tip_count:number }
export interface Receipt { payload:{policy_digest:string;capability_digest:string;catalog_commit?:string;catalog_digest:string;catalog_provenance:ReceiptCatalogProvenance;cited_tips:string[];source_commit:string;source_digest:string;rule_result:Verdict["status"];replay_digest:string;replay_result:unknown}; signature:string }
const rules:Record<string,string>={R1:"require-human-approval-for-irreversible-actions",R2:"allowlist-tools-per-agent-role",R3:"set-a-token-and-cost-budget-per-agent-run",R4:"set-a-token-and-cost-budget-per-agent-run",R5:"add-a-max-step-budget-to-every-agent",R6:"set-wall-clock-timeouts-for-agent-runs",R7:"keep-a-kill-switch-for-agent-actions",R8:"block-ssrf-by-validating-outbound-urls",R9:"sandbox-model-generated-code-execution",R10:"cap-agent-tool-retries",R11:"budget-context-before-adding-tools"};
const impact:Record<string,number>={transformative:4,high:3,medium:2,low:1}; const confidence:Record<string,number>={"production-verified":1,"lab-verified":.8,"community-reported":.6,theoretical:.3};
export const canonical=(v:unknown):string=>Array.isArray(v)?`[${v.map(canonical).join(",")}]`:v&&typeof v==="object"?`{${Object.keys(v as object).sort().map(k=>`${JSON.stringify(k)}:${canonical((v as Record<string,unknown>)[k])}`).join(",")}}`:JSON.stringify(v);
export const digest=(v:unknown)=>createHash("sha256").update(canonical(v)).digest("hex"); export const fileDigest=(p:string)=>createHash("sha256").update(readFileSync(p)).digest("hex");
const scalar=(v:string):unknown=>v==="null"?null:v==="true"?true:v==="false"?false:/^[-+]?\d+(\.\d+)?$/.test(v)?Number(v):v.replace(/^['"]|['"]$/g,""); const list=(v:string):string[]=>v.trim()==="[]"?[]:v.trim().replace(/^\[|\]$/g,"").split(",").map(x=>x.trim().replace(/^['"]|['"]$/g,"")).filter(Boolean);
export function parsePolicyYaml(text:string):Policy { const root:any={roles:[],runtime:{},guardrails:{},claims:[]};let section="",role:any; for(const raw of text.split(/\r?\n/)){const line=raw.replace(/\s+#.*$/,"");if(!line.trim())continue;const indent=line.match(/^\s*/)?.[0].length||0,m=line.trim().match(/^([^:]+):\s*(.*)$/);if(!m){const i=line.trim().match(/^-\s*(.+)$/);if(i&&section==="claims")root.claims.push(String(scalar(i[1])));continue;}const[,key,value]=m;if(indent===0&&key==="policy")continue;if(indent===2&&["roles","runtime","guardrails","claims"].includes(key)){section=key;if(key==="claims"&&value)root.claims=list(value);continue;}if(section==="roles"&&indent===4&&key==="- id"){role={id:String(scalar(value))};root.roles.push(role);continue;}if(section==="roles"&&indent>=6&&role){role[key]=value.startsWith("[")?list(value):scalar(value);continue;}if((section==="runtime"||section==="guardrails")&&indent>=4){root[section][key]=value.startsWith("[")?list(value):scalar(value);continue;}if(indent===2)root[key]=value.startsWith("[")?list(value):scalar(value);}if(!root.name||!Array.isArray(root.roles))throw new Error("invalid policy YAML: policy.name and policy.roles are required");return root; }
function git(args:string[]):string|undefined{try{return execFileSync("git",args,{encoding:"utf8",stdio:["ignore","pipe","ignore"]}).trim();}catch{return undefined;}}
export function sourceBinding(sourcePath:string):{root:string;commit:string}{const root=git(["-C",dirname(resolve(sourcePath)),"rev-parse","--show-toplevel"]);const value=root?git(["-C",root,"rev-parse","HEAD"]):undefined;if(!root||!value||!/^[a-f0-9]{40}$/i.test(value))throw new Error("source is outside a Git worktree");return{root,commit:value};}
export function sourceCommit(cwd=process.cwd()):string { const value=git(["-C",cwd,"rev-parse","HEAD"]);if(!value||!/^[a-f0-9]{40}$/i.test(value))throw new Error("source Git commit unavailable");return value; }
export function loadCatalog(path=process.env.ARSENAL_DATA_PATH||"fixtures/catalog"):Catalog { const full=resolve(path),tipsPath=resolve(full,"tips.json"),bytes=readFileSync(tipsPath),data=JSON.parse(bytes.toString());const root=git(["-C",full,"rev-parse","--show-toplevel"]),head=root?git(["-C",root,"rev-parse","HEAD"]):undefined;const real=!!head&&resolve(full)===resolve(root!,"data");let policyPin:string|undefined;try{policyPin=JSON.parse(readFileSync(resolve("rules.map.json"),"utf8")).meta.catalog_commit;}catch{}const provenance={source_type:real?"git" as const:"fixture" as const,resolved_path:full,...(real?{git_commit:head}:{}),content_sha256:createHash("sha256").update(bytes).digest("hex"),...(data.generated_at?{generated_at:data.generated_at}:{}),tip_count:Array.isArray(data.items)?data.items.length:0};return {commit:head&&real?head:policyPin,policyPin:real?head:policyPin,contentDigest:provenance.content_sha256,provenance,tips:new Map(data.items.map((t:Tip)=>[t.id,t]))}; }
export function catalogProvenance(catalog:Catalog):ReceiptCatalogProvenance {return{source_type:catalog.provenance.source_type,...(catalog.commit?{catalog_commit:catalog.commit}:{}),content_sha256:catalog.contentDigest,...(catalog.provenance.generated_at?{generated_at:catalog.provenance.generated_at}:{}),tip_count:catalog.provenance.tip_count};}
const finite=(v:unknown)=>typeof v==="number"&&Number.isFinite(v)&&v>0;
export function check(policy:Policy,catalog:Catalog,policyName="policy"):Verdict { if(policy.catalog_commit&&catalog.commit&&policy.catalog_commit!==catalog.commit)throw new Error(`catalog pin mismatch: policy=${policy.catalog_commit}, loaded=${catalog.commit}`);for(const id of Object.values(rules))if(!catalog.tips.has(id))throw new Error(`unknown/fabricated Arsenal tip: ${id}`);for(const id of policy.claims)if(!catalog.tips.has(id)||!Object.values(rules).includes(id))throw new Error(`unknown/fabricated Arsenal tip: ${id}`);const f:{rule:string;evidence:string}[]=[],r=policy.runtime,g=policy.guardrails,irreversible=[...new Set(policy.roles.flatMap(x=>x.irreversible_tools||[]))],approvals=(g.human_approval_for as string[])||[],missing=irreversible.filter(x=>!approvals.includes(x));if(missing.length)f.push({rule:"R1",evidence:missing.length>1?`irreversible tools ${missing.map(x=>`'${x}'`).join(",")} exposed without human_approval_for gate`:`irreversible tool '${missing[0]}' exposed without human_approval_for gate`});for(const role of policy.roles)if(!role.tools?.length)f.push({rule:"R2",evidence:`role '${role.id}' exposes tools without an explicit allowlist`});if(!finite(r.token_budget))f.push({rule:"R3",evidence:"token_budget is null/0; token spend unbounded"});if(!finite(r.cost_budget_usd))f.push({rule:"R4",evidence:"cost_budget_usd is null/0; spend uncapped"});if(!finite(r.max_steps))f.push({rule:"R5",evidence:"max_steps is null/unbounded; agent can loop indefinitely"});if(!finite(r.wall_clock_timeout_sec))f.push({rule:"R6",evidence:"wall_clock_timeout_sec is null; run can hang"});if(g.kill_switch!==true)f.push({rule:"R7",evidence:"kill_switch is false; cannot halt runaway agent"});if(g.ssrf_guard!==true)f.push({rule:"R8",evidence:"ssrf_guard is false; outbound tool URLs unvalidated"});const code=(r.code_exec_tools as string[])||[];if(code.length&&g.sandbox_code_exec!==true)f.push({rule:"R9",evidence:`code-executing tool(s) ${code.join(", ")} present without sandbox_code_exec`});if(!finite(r.retry_cap))f.push({rule:"R10",evidence:"retry_cap is null; agent can retry a broken tool infinitely"});const count=new Set(policy.roles.flatMap(x=>x.tools||[])).size;if(count>8&&(!finite(r.token_budget)||r.context_compaction!==true))f.push({rule:"R11",evidence:`policy exposes ${count} tools with no context_compaction; context-overflow risk`});const order=["R1","R5","R7","R10","R2","R3","R4","R6","R8","R9","R11"];f.sort((a,b)=>order.indexOf(a.rule)-order.indexOf(b.rule));const findings=f.map(x=>{const t=catalog.tips.get(rules[x.rule])!;return{rule:x.rule,arsenal_tip_id:t.id,severity:t.impact,weight:Number((impact[t.impact]*confidence[t.verification_status]).toFixed(1)),evidence:x.evidence,verification_status:t.verification_status};});const failed=new Set(findings.map(x=>x.arsenal_tip_id)),unsubstantiated=policy.claims.filter(x=>failed.has(x)),cited=[...new Set([...findings.map(x=>x.arsenal_tip_id),...policy.claims,...(irreversible.length?[rules.R1]:[]),rules.R3,rules.R4,rules.R5,rules.R6,rules.R7,rules.R8,rules.R10,...(count>8?[rules.R11]:[])])].sort(),badges=cited.map(id=>{const t=catalog.tips.get(id)!;return{arsenal_tip_id:id,impact:t.impact,verification_status:t.verification_status,enrichment_status:t.enrichment_status};}),substantiated=policy.claims.filter(x=>!unsubstantiated.includes(x)),status:Verdict["status"]=findings.length?"FAIL":unsubstantiated.length||(substantiated.length>0&&!substantiated.some(id=>{const t=catalog.tips.get(id)!;return t.verification_status==="production-verified"||t.enrichment_status==="reviewed";}))?"WARN":"PASS",remediation=findings.length===2&&findings.some(x=>x.rule==="R11")?{ranked:findings.slice().sort((a,b)=>b.weight-a.weight).map(x=>x.rule==="R1"?{rule:x.rule,weight:x.weight,resolution:`add guardrails.human_approval_for: [${irreversible.join(", ")}]`}:{rule:x.rule,weight:x.weight,resolution:"add runtime.context_compaction: true (or reduce tool breadth)"}),resolution_set:findings.slice().sort((a,b)=>b.weight-a.weight).map(x=>x.rule==="R1"?`add guardrails.human_approval_for: [${irreversible.join(", ")}]  (resolves R1, blast_radius ${x.weight.toFixed(1)})`:`add runtime.context_compaction: true  (resolves R11, blast_radius ${x.weight.toFixed(1)})`),triggered_rules:findings.slice().sort((a,b)=>b.weight-a.weight).map(x=>x.rule)}:null,seal=createHash("sha256").update(`${canonical(policy)}\n${cited.join(",")}\n${catalog.commit||catalog.contentDigest}`).digest("hex");return{policy:policyName,status,findings,unsubstantiated_claims:unsubstantiated,cited_tips:cited,seal,remediation_plan:remediation,trust_badges:badges}; }
const lit=(n:ts.Expression|undefined):unknown=>!n?undefined:ts.isStringLiteral(n)?n.text:ts.isNumericLiteral(n)?Number(n.text):n.kind===ts.SyntaxKind.TrueKeyword?true:n.kind===ts.SyntaxKind.FalseKeyword?false:n.kind===ts.SyntaxKind.NullKeyword?null:ts.isArrayLiteralExpression(n)?n.elements.map(x=>lit(x as ts.Expression)):undefined;
function scanManifestUnknown(root:ts.ObjectLiteralExpression,sf:ts.SourceFile,out:Capabilities,add:(kind:Evidence["kind"],detail:string,node:ts.Node)=>void){const scan=(node:ts.Node)=>{if(ts.isSpreadAssignment(node)){const detail="spread assignment may override manifest security fields";out.unsupported.push(detail);add("unknown",detail,node);return;}if(ts.isComputedPropertyName(node)){const detail="computed manifest property is unsupported";out.unsupported.push(detail);add("unknown",detail,node);return;}if(ts.isShorthandPropertyAssignment(node)){const detail="shorthand/dynamic manifest property is unsupported";out.unsupported.push(detail);add("unknown",detail,node);return;}ts.forEachChild(node,scan);};scan(root);}
export function extract(file:string):Capabilities {const sf=ts.createSourceFile(file,readFileSync(file,"utf8"),ts.ScriptTarget.Latest,true),out:Capabilities={roles:[],runtime:{},guardrails:{},network_tools:[],code_exec_tools:[],approval_wrappers:[],evidence:[],unsupported:[]},add=(kind:Evidence["kind"],detail:string,n:ts.Node)=>out.evidence.push({file,line:sf.getLineAndCharacterOfPosition(n.getStart(sf)).line+1,kind,detail}),prop=(o:ts.ObjectLiteralExpression,k:string)=>o.properties.find(p=>ts.isPropertyAssignment(p)&&p.name.getText(sf).replace(/["']/g,"")===k)as ts.PropertyAssignment|undefined;const visit=(n:ts.Node)=>{if(ts.isCallExpression(n)&&n.expression.getText(sf)==="defineAgent"){const first=n.arguments[0];if(!first||!ts.isObjectLiteralExpression(first)){out.unsupported.push("defineAgent requires a literal object");add("unknown","dynamic defineAgent manifest",n);}else{scanManifestUnknown(first,sf,out,add);const roles=prop(first,"roles")?.initializer;if(!roles||!ts.isArrayLiteralExpression(roles)){out.unsupported.push("defineAgent.roles must be a literal array");add("unknown","dynamic roles",first);}else for(const e of roles.elements){if(!ts.isObjectLiteralExpression(e)){out.unsupported.push("dynamic role");continue;}const id=lit(prop(e,"id")?.initializer),tools=lit(prop(e,"tools")?.initializer),irr=lit(prop(e,"irreversible_tools")?.initializer);if(typeof id!=="string"||!Array.isArray(tools)){out.unsupported.push("role requires literal id and tools");add("unknown","dynamic role fields",e);continue;}out.roles.push({id,tools:tools as string[],irreversible_tools:Array.isArray(irr)?irr as string[]:[]});add("declared",`role ${id}`,e);}for(const key of ["runtime","guardrails"]as const){const o=prop(first,key)?.initializer;if(o&&ts.isObjectLiteralExpression(o))for(const p of o.properties)if(ts.isPropertyAssignment(p)){const value=lit(p.initializer);if(value===undefined){out.unsupported.push(`dynamic ${key}.${p.name.getText(sf)}`);add("unknown",`dynamic ${key}.${p.name.getText(sf)}`,p);}else{out[key][p.name.getText(sf)]=value;add("declared",`${key}.${p.name.getText(sf)}`,p);}}}out.code_exec_tools=((out.runtime.code_exec_tools as string[])||[]);out.network_tools=((out.runtime.network_tools as string[])||[]);out.approval_wrappers=((out.guardrails.human_approval_for as string[])||[]);for(const role of out.roles)for(const tool of role.irreversible_tools)add("inferred",`irreversible capability ${tool}`,first);}}ts.forEachChild(n,visit);};visit(sf);return out;}
export function consistency(policy:Policy,cap:Capabilities):ConsistencyFinding[]{const f:ConsistencyFinding[]=[];for(const unknown of cap.unsupported){const e=cap.evidence.find(x=>x.kind==="unknown"&&x.detail.includes(unknown.split(" ")[0]));f.push({code:"SOURCE_EXTRACTION_UNKNOWN",evidence:`${e?`${e.file}:${e.line} `:""}${unknown}`});}const policyRoles=new Map(policy.roles.map(r=>[r.id,r])),sourceRoles=new Map(cap.roles.map(r=>[r.id,r]));for(const role of policy.roles)if(!sourceRoles.has(role.id))f.push({code:"policy-role-absent-from-source",evidence:`policy role '${role.id}' absent from source`});for(const role of cap.roles){const declared=policyRoles.get(role.id);if(!declared){f.push({code:"source-role-absent",evidence:`source role '${role.id}' absent from policy`});continue;}for(const t of new Set([...(role.tools||[]),...(declared.tools||[])]))if((role.tools||[]).includes(t)!==(declared.tools||[]).includes(t))f.push({code:"role-tool-mismatch",evidence:`role '${role.id}' tool '${t}' differs between policy and source`});for(const t of new Set([...(role.irreversible_tools||[]),...(declared.irreversible_tools||[])]))if((role.irreversible_tools||[]).includes(t)!==(declared.irreversible_tools||[]).includes(t))f.push({code:"irreversible-mismatch",evidence:`role '${role.id}' irreversible tool '${t}' differs between policy and source`});}const approval=(policy.guardrails.human_approval_for as string[])||[];if(canonical(approval)!==canonical(cap.approval_wrappers))f.push({code:"source-approval-wrapper-absent",evidence:`policy approval: ${JSON.stringify(approval)}; source approval: ${JSON.stringify(cap.approval_wrappers)}`,policy_value:approval,source_value:cap.approval_wrappers});for(const [section,expected,actual] of [["runtime",policy.runtime,cap.runtime],["guardrails",policy.guardrails,cap.guardrails]] as const)for(const key of new Set([...Object.keys(expected),...Object.keys(actual)]))if(canonical(expected[key])!==canonical(actual[key]))f.push({code:`${section}-disagreement`,evidence:`policy/source ${section}.${key} disagree`,policy_value:expected[key],source_value:actual[key]});if(canonical((policy.runtime.network_tools as string[])||[])!==canonical(cap.network_tools))f.push({code:"network-capability-mismatch",evidence:"network capabilities differ"});if(canonical((policy.runtime.code_exec_tools as string[])||[])!==canonical(cap.code_exec_tools))f.push({code:"code-exec-capability-mismatch",evidence:"code execution capabilities differ"});return f;}
export function attestableCapabilityReasons(cap:Capabilities,policy?:Policy):string[]{const reasons:string[]=[];const add=(detail:string)=>reasons.push(`unsupported source capability evidence: ${detail}`);for(const detail of cap.unsupported)add(detail);for(const evidence of cap.evidence)if(evidence.kind==="unknown")add(evidence.detail);if(!cap.evidence.length)add("no extracted source evidence");if(!cap.roles.length)add("no extracted roles");for(const role of cap.roles)if(!role.id||!Array.isArray(role.tools))add("incomplete role/tool evidence");if(policy)for(const finding of consistency(policy,cap))add(`${finding.code}: ${finding.evidence}`);return[...new Set(reasons)];}
export function assertAttestableCapabilities(cap:Capabilities,policy?:Policy):void{const reasons=attestableCapabilityReasons(cap,policy);if(reasons.length)throw new Error(reasons.join("; "));}
export function diff(before:Capabilities,after:Capabilities){type Edge={role:string;tool:string;irreversible:boolean};const edges=(c:Capabilities)=>new Map(c.roles.flatMap(r=>r.tools.map(t=>[`${r.id}\0${t}`,{role:r.id,tool:t,irreversible:r.irreversible_tools.includes(t)}]as const))),b=edges(before),a=edges(after),added=[...a].filter(([k])=>!b.has(k)).map(([,e])=>e),removed=[...b].filter(([k])=>!a.has(k)).map(([,e])=>e),moved=added.flatMap(to=>removed.filter(from=>from.tool===to.tool).map(from=>({tool:to.tool,from:from.role,to:to.role}))),flagChanges=[...a].flatMap(([k,e])=>b.has(k)&&b.get(k)!.irreversible!==e.irreversible?[{role:e.role,tool:e.tool,from:b.get(k)!.irreversible,to:e.irreversible}]:[]),changes=(x:Record<string,unknown>,y:Record<string,unknown>)=>Object.keys({...x,...y}).filter(k=>canonical(x[k])!==canonical(y[k]));return{added_tools:added,removed_tools:removed,moved_tools:moved,irreversible_added:flagChanges.filter(x=>x.to),irreversible_removed:flagChanges.filter(x=>!x.to),network_added:after.network_tools.filter(x=>!before.network_tools.includes(x)),network_removed:before.network_tools.filter(x=>!after.network_tools.includes(x)),code_exec_added:after.code_exec_tools.filter(x=>!before.code_exec_tools.includes(x)),code_exec_removed:before.code_exec_tools.filter(x=>!after.code_exec_tools.includes(x)),approval_added:after.approval_wrappers.filter(x=>!before.approval_wrappers.includes(x)),approval_removed:before.approval_wrappers.filter(x=>!after.approval_wrappers.includes(x)),guardrail_changes:changes(before.guardrails,after.guardrails),runtime_changes:changes(before.runtime,after.runtime),unknown:[...before.unsupported,...after.unsupported],evidence:{before:before.evidence,after:after.evidence}};}
class PolicyRunner {private steps=0;private retries=new Map<string,number>();private tokens=0;private cost=0;private started=Date.now();private readonly policy:Policy;constructor(policy:Policy){this.policy=policy;}call(role:string,tool:string,input:{approved?:boolean;tokens?:number;cost?:number}={}){const g=this.policy.guardrails,r=this.policy.roles.find(x=>x.id===role);if(g.kill_switch!==true)throw new Error("blocked: kill switch absent");if(!r?.tools?.includes(tool))throw new Error("blocked: tool is not allowlisted");if((r.irreversible_tools||[]).includes(tool)&&(!input.approved||!((g.human_approval_for as string[])||[]).includes(tool)))throw new Error("blocked: approval required");if((this.policy.runtime.network_tools as string[]||[]).length&&g.ssrf_guard!==true)throw new Error("blocked: SSRF guard absent");if((this.policy.runtime.code_exec_tools as string[]||[]).length&&g.sandbox_code_exec!==true)throw new Error("blocked: sandbox absent");if(++this.steps>(this.policy.runtime.max_steps as number))throw new Error("blocked: max steps");if(Date.now()-this.started>(this.policy.runtime.wall_clock_timeout_sec as number)*1000)throw new Error("blocked: timeout");const n=(this.retries.get(tool)||0)+1;this.retries.set(tool,n);if(n>(this.policy.runtime.retry_cap as number))throw new Error("blocked: retry cap");this.tokens+=input.tokens||0;this.cost+=input.cost||0;if(this.tokens>(this.policy.runtime.token_budget as number)||this.cost>(this.policy.runtime.cost_budget_usd as number))throw new Error("blocked: budget");return{executed:tool};}}
export function replay(policy:Policy,trace:string,guarded:boolean){const events=readFileSync(trace,"utf8").trim().split(/\n/).filter(Boolean).map(x=>JSON.parse(x)),runner=guarded?new PolicyRunner(policy):undefined;return events.map((e:any,index:number)=>{try{return{index:index+1,tool:e.tool,result:"executed",out:guarded?runner!.call(e.role,e.tool,e):{executed:e.tool}}}catch(error){return{index:index+1,tool:e.tool,result:"blocked",reason:(error as Error).message}}});}
export function replayProof(policy:Policy,trace:string){return{unsafe:replay(policy,trace,false),guarded:replay(policy,trace,true)};}
export function receipt(policy:Policy,capability:Capabilities,verdict:Verdict,replayResult:unknown,privateKey:string,catalog:Catalog,commit=sourceCommit(),sourceDigest?:string):Receipt {if(!sourceDigest)throw new Error("source digest is required for attestation");const payload={policy_digest:digest(policy),capability_digest:digest(capability),...(catalog.commit?{catalog_commit:catalog.commit}:{}),catalog_digest:catalog.contentDigest,catalog_provenance:catalogProvenance(catalog),cited_tips:[...verdict.cited_tips].sort(),source_commit:commit,source_digest:sourceDigest,rule_result:verdict.status,replay_digest:digest(replayResult),replay_result:replayResult};return{payload:payload as Receipt["payload"],signature:sign(null,Buffer.from(canonical(payload)),privateKey).toString("base64")};}
export function verifyReceipt(receiptValue:unknown,publicKey:string,inputs:{policy:Policy;capability:Capabilities;replay:unknown;catalog:Catalog;sourceCommit?:string;sourceDigest?:string}):{verified:boolean;reasons:string[];verdict?:Verdict}{const reasons:string[]=[];const receipt=receiptValue as Receipt;if(!receipt||typeof receipt!=="object"||!receipt.payload||typeof receipt.signature!=="string"){return{verified:false,reasons:["malformed receipt"]};}const p=receipt.payload;if(!verify(null,Buffer.from(canonical(p)),publicKey,Buffer.from(receipt.signature,"base64")))reasons.push("invalid signature");const verdict=check(inputs.policy,inputs.catalog,"verify");reasons.push(...attestableCapabilityReasons(inputs.capability,inputs.policy));if(p.policy_digest!==digest(inputs.policy))reasons.push("policy digest mismatch");if(p.capability_digest!==digest(inputs.capability))reasons.push("capability digest mismatch");if(p.catalog_digest!==inputs.catalog.contentDigest||p.catalog_commit!==inputs.catalog.commit||canonical(p.catalog_provenance)!==canonical(catalogProvenance(inputs.catalog)))reasons.push("catalog provenance mismatch");if(p.rule_result!==verdict.status)reasons.push("rule result mismatch");if(canonical(p.cited_tips)!==canonical(verdict.cited_tips))reasons.push("cited tips mismatch");if(p.replay_digest!==digest(inputs.replay)||canonical(p.replay_result)!==canonical(inputs.replay))reasons.push("replay result mismatch");const actual=inputs.sourceCommit||sourceCommit();if(!/^[a-f0-9]{40}$/i.test(p.source_commit)||p.source_commit!==actual)reasons.push("source commit mismatch");if(!inputs.sourceDigest||p.source_digest!==inputs.sourceDigest)reasons.push("source digest mismatch");return{verified:reasons.length===0,reasons:[...new Set(reasons)],verdict};}
export class Guard {private runner?:PolicyRunner;private readonly policy:Policy;private readonly capability:Capabilities;private readonly receipt?:Receipt;private readonly publicKey?:string;private readonly catalog?:Catalog;private readonly trace?:string;private readonly boundSourceCommit?:string;private readonly boundSourceDigest?:string;private readonly sourcePath?:string;constructor(policy:Policy,capability:Capabilities=capabilityFromPolicy(policy),receipt?:Receipt,publicKey?:string,catalog?:Catalog,trace?:string,sourceCommitValue?:string,sourceDigestValue?:string,sourcePath?:string){this.policy=policy;this.capability=capability;this.receipt=receipt;this.publicKey=publicKey;this.catalog=catalog;this.trace=trace;this.boundSourceCommit=sourceCommitValue;this.boundSourceDigest=sourceDigestValue;this.sourcePath=sourcePath;} private bind(){if(this.runner)return;if(!this.receipt||!this.publicKey||!this.catalog||!this.trace||!this.sourcePath)throw new Error("fail closed: missing receipt binding");const extracted=extract(this.sourcePath);assertAttestableCapabilities(extracted,this.policy);if(digest(extracted)!==digest(this.capability))throw new Error("fail closed: source capability digest mismatch");const binding=sourceBinding(this.sourcePath),sourceDigestValue=fileDigest(this.sourcePath);if(this.boundSourceCommit&&this.boundSourceCommit!==binding.commit)throw new Error("fail closed: source commit input mismatch");if(this.boundSourceDigest&&this.boundSourceDigest!==sourceDigestValue)throw new Error("fail closed: source digest input mismatch");const result=verifyReceipt(this.receipt,this.publicKey,{policy:this.policy,capability:extracted,replay:replayProof(this.policy,this.trace),catalog:this.catalog,sourceCommit:binding.commit,sourceDigest:sourceDigestValue});if(!result.verified)throw new Error(`fail closed: ${result.reasons.join(", ")}`);if(this.receipt.payload.rule_result!=="PASS")throw new Error("fail closed: receipt is not PASS");this.runner=new PolicyRunner(this.policy);} call(role:string,tool:string,input:{approved?:boolean;tokens?:number;cost?:number}={}){this.bind();return this.runner!.call(role,tool,input);}}
export function capabilityFromPolicy(policy:Policy):Capabilities{return{roles:policy.roles.map(r=>({id:r.id,tools:r.tools||[],irreversible_tools:r.irreversible_tools||[]})),runtime:{...policy.runtime},guardrails:{...policy.guardrails},network_tools:(policy.runtime.network_tools as string[])||[],code_exec_tools:(policy.runtime.code_exec_tools as string[])||[],approval_wrappers:(policy.guardrails.human_approval_for as string[])||[],evidence:[],unsupported:[]};}
export function keygenFiles(dir:string,force=false){const priv=resolve(dir,"private.pem"),pub=resolve(dir,"public.pem");if(!force&&(existsSync(priv)||existsSync(pub)))throw new Error("key files already exist; use --force");const keys=generateKeyPairSync("ed25519"),privateKey=keys.privateKey.export({type:"pkcs8",format:"pem"}).toString(),publicKey=keys.publicKey.export({type:"spki",format:"pem"}).toString();const fd=openSync(priv,"w",0o600);writeFileSync(fd,privateKey);chmodSync(priv,0o600);writeFileSync(pub,publicKey,{mode:0o644});chmodSync(pub,0o644);return{privateKeyPath:priv,publicKeyPath:pub};}
export function generatedGuardSource(policy:Policy,capabilityDigest:string,catalog:Catalog){const p=JSON.stringify(policy),pd=JSON.stringify(digest(policy)),cd=JSON.stringify(capabilityDigest),commit=JSON.stringify(catalog.commit),catalogDigest=JSON.stringify(catalog.contentDigest),provenance=JSON.stringify(catalogProvenance(catalog));return `import { createHash, verify } from "node:crypto";
export const policy=${p}; export const policyDigest=${pd}; export const expectedCapabilityDigest=${cd}; export const catalogCommit=${commit}; export const catalogDigest=${catalogDigest}; export const catalogProvenance=${provenance};
const canonical=v=>Array.isArray(v)?"["+v.map(canonical).join(",")+"]":v&&typeof v==="object"?"{"+Object.keys(v).sort().map(k=>JSON.stringify(k)+":"+canonical(v[k])).join(",")+"}":JSON.stringify(v); const digest=v=>createHash("sha256").update(canonical(v)).digest("hex");
const capabilityReasons=cap=>{const reasons=[],add=detail=>reasons.push("unsupported source capability evidence: "+detail);if(!cap||typeof cap!=="object")return ["unsupported source capability evidence: missing extracted capabilities"];for(const detail of cap.unsupported||[])add(detail);for(const evidence of cap.evidence||[])if(evidence.kind==="unknown")add(evidence.detail);if(!Array.isArray(cap.evidence)||!cap.evidence.length)add("no extracted source evidence");if(!Array.isArray(cap.roles)||!cap.roles.length)add("no extracted roles");for(const expected of policy.roles){const actual=(cap.roles||[]).find(role=>role.id===expected.id);if(!actual||canonical(actual.tools||[])!==canonical(expected.tools||[])||canonical(actual.irreversible_tools||[])!==canonical(expected.irreversible_tools||[]))add("incomplete role/tool evidence for "+expected.id);}for(const section of ["runtime","guardrails"])for(const key of new Set([...Object.keys(policy[section]),...Object.keys(cap[section]||{})]))if(canonical(policy[section][key])!==canonical((cap[section]||{})[key]))add("incomplete "+section+" evidence for "+key);return [...new Set(reasons)];};
export function verifyBinding(receipt,publicKey,binding){if(!receipt||!receipt.payload||typeof receipt.signature!=="string"||!binding||typeof binding.sourceCommit!=="string"||typeof binding.sourceDigest!=="string"||!Array.isArray(binding.citedTips)||!Object.hasOwn(binding,"replayResult")||!Object.hasOwn(binding,"capabilities"))return {ok:false,reason:"missing receipt binding context"};const capabilityFailure=capabilityReasons(binding.capabilities);if(capabilityFailure.length)return {ok:false,reason:capabilityFailure[0]};const p=receipt.payload;if(digest(binding.capabilities)!==expectedCapabilityDigest||p.policy_digest!==policyDigest||p.capability_digest!==expectedCapabilityDigest||p.catalog_commit!==catalogCommit||p.catalog_digest!==catalogDigest||canonical(p.catalog_provenance)!==canonical(catalogProvenance)||p.source_commit!==binding.sourceCommit||p.source_digest!==binding.sourceDigest||canonical(p.cited_tips)!==canonical([...binding.citedTips].sort())||p.rule_result!=="PASS"||p.replay_digest!==digest(binding.replayResult)||canonical(p.replay_result)!==canonical(binding.replayResult))return {ok:false,reason:"receipt binding mismatch"};return verify(null,Buffer.from(canonical(p)),publicKey,Buffer.from(receipt.signature,"base64"))?{ok:true}:{ok:false,reason:"invalid signature"};}
export class GeneratedGuard {constructor(receipt,publicKey,binding){const b=verifyBinding(receipt,publicKey,binding);if(!b.ok)throw new Error(\`fail closed: \${b.reason}\`);this.steps=0;this.retries=new Map();this.tokens=0;this.cost=0;this.started=Date.now();}call(role,tool,input={}){const r=policy.roles.find(x=>x.id===role);if(policy.guardrails.kill_switch!==true)throw new Error("blocked: kill switch absent");if(!r||!r.tools.includes(tool))throw new Error("blocked: tool is not allowlisted");if((r.irreversible_tools||[]).includes(tool)&&(!input.approved||!policy.guardrails.human_approval_for.includes(tool)))throw new Error("blocked: approval required");if((policy.runtime.network_tools||[]).length&&!policy.guardrails.ssrf_guard)throw new Error("blocked: SSRF guard absent");if((policy.runtime.code_exec_tools||[]).length&&!policy.guardrails.sandbox_code_exec)throw new Error("blocked: sandbox absent");if(++this.steps>policy.runtime.max_steps)throw new Error("blocked: max steps");if(Date.now()-this.started>policy.runtime.wall_clock_timeout_sec*1000)throw new Error("blocked: timeout");const n=(this.retries.get(tool)||0)+1;this.retries.set(tool,n);if(n>policy.runtime.retry_cap)throw new Error("blocked: retry cap");this.tokens+=input.tokens||0;this.cost+=input.cost||0;if(this.tokens>policy.runtime.token_budget||this.cost>policy.runtime.cost_budget_usd)throw new Error("blocked: budget");return {executed:tool};}}
`;}
const esc=(s:string)=>s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;");
export function htmlReport(v:Verdict,timeline:unknown,cap?:Capabilities,catalog?:Catalog,receiptStatus?:unknown,d?:unknown){const block=(x:unknown)=>esc(canonical(x));return `<!doctype html><html><head><meta charset="utf-8"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'"><title>ArsenalLint PR Report</title><style>body{font:15px system-ui;max-width:1000px;margin:2rem auto;color:#172033}pre{background:#f3f6fa;padding:1rem;overflow:auto}</style></head><body><h1>ArsenalLint proof report — ${esc(v.status)}</h1><p>Supported manifest/code path checks and deterministic replay only; not a general safety or compliance proof.</p><h2>Capability diff</h2><pre>${block(d||{})}</pre><h2>Source evidence</h2><pre>${block(cap?.evidence||[])}</pre><h2>Findings and remediation plan</h2><pre>${block({findings:v.findings,remediation_plan:v.remediation_plan})}</pre><h2>Catalog trust badges and provenance</h2><pre>${block({badges:v.trust_badges,provenance:catalog?catalogProvenance(catalog):undefined})}</pre><h2>Replay timeline</h2><pre>${block(timeline)}</pre><h2>Receipt signature verification</h2><pre>${block(receiptStatus||"not verified")}</pre></body></html>`;}
